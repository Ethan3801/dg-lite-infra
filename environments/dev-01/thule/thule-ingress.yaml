apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: thule
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    ingress.kubernetes.io/force-ssl-redirect: "true"
    kubernetes.io/tls-acme: "true"
    external-dns.alpha.kubernetes.io/hostname: api.dev.dglite.com
    external-dns.alpha.kubernetes.io/ttl: 10
spec:
  tls:
    - secretName: thule
      hosts:
        - api.dev.dglite.com
        - api.dglite-aws.danward.me
  rules:
    - host: api.dev.dglite.com
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: thule
                port:
                  number: 3000
    - host: api.dglite-aws.danward.me
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: thule
                port:
                  number: 3000
